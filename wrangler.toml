# hellodk.io — Cloudflare Pages (Git build + wrangler pages deploy)
# Do not add [assets] here — that is Workers-only and breaks Pages validation.
name = "cfpages"
compatibility_date = "2024-06-01"
pages_build_output_dir = "dist"

[ai]
binding = "AI"
